name: Temp Format P0 Followup

on:
  push:
    branches:
      - codex/p0-audit-correctness-fixes
    paths:
      - .github/workflows/temp-format-p0-followup.yml

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex/p0-audit-correctness-fixes

      - uses: ./.github/actions/setup-toolchain

      - name: Run repository setup
        run: ./tool/setup.sh

      - name: Format follow-up Flutter changes
        working-directory: apps/butlerly
        run: |
          dart format             integration_test/support/v1_integration_harness.dart             lib/features/foundation/presentation/statement_capture_page.dart             test/core/database/system_catalog_reseed_test.dart             test/design_system/butlerly_category_identity_test.dart             test/features/foundation/statement_capture_page_test.dart

      - name: Commit formatter output
        run: |
          if git diff --quiet; then
            echo "No formatting changes."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add             apps/butlerly/integration_test/support/v1_integration_harness.dart             apps/butlerly/lib/features/foundation/presentation/statement_capture_page.dart             apps/butlerly/test/core/database/system_catalog_reseed_test.dart             apps/butlerly/test/design_system/butlerly_category_identity_test.dart             apps/butlerly/test/features/foundation/statement_capture_page_test.dart
          git commit -m "Format P0 audit follow-up fixes"
          git push origin HEAD:codex/p0-audit-correctness-fixes
