# macOS deployment

Run:

```sh
./deploy.sh
```

The script builds and opens Butlerly. To keep it installed, drag the resulting
`butlerly.app` into Applications. If Gatekeeper warns on first launch,
Control-click the app and choose **Open**.
