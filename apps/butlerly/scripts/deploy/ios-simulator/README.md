# iOS Simulator deployment

Start an iOS Simulator, then run:

```sh
./deploy.sh
```

To target a specific running simulator:

```sh
./deploy.sh <simulator-udid>
```

The script builds the current source, installs it, and launches Butlerly.
