# Web device testing

Run:

```sh
./deploy.sh
```

The default port is 8080. To choose another port, run `./deploy.sh 9000`.
Keep the terminal open while testing. Devices on the same Wi-Fi can browse to
the Mac's local IP address and selected port.
